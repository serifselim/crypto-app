import React from 'react';
import { NavContainer, NavContent, NavItem } from './Nav.styled';

const Nav = () => {
    return (
        <NavContainer>
            <NavContent>
                <NavItem>

                </NavItem>
            </NavContent>
        </NavContainer>
    );
};

export default Nav;